/* ---------------------------------------------------------------------- */
/*                      Calculation of Cube Position                      */
/* ---------------------------------------------------------------------- */

#include <sim3d.h>


//NOTE: start writing updating calculations 

void cube::updatePos(Directions direction){
	switch(direction) {
		case UP: { return; }
		case DOWN: { return; }
		case LEFT: { return; }
		case RIGHT: { return; }
	}
}
